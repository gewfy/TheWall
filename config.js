module.exports = {
  /* Server port */
  port:           8000,

  /* Use a different domain for each message */
  domainSecurity: true,

  /* Amount of simultaneous messages in the clients */
  maxMessages:    2
};